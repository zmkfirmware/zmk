description: |
  Allows defining a keymap composed of multiple layers

compatible: "zmk,keymap"

properties:
  label:
    type: string
    required: true

  layers:
    type: phandles
    required: true
