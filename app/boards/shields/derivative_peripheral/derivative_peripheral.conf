# Optional overrides for the derivative keyboard in split-peripheral (dongle)
# mode. Role and Studio handling come from Kconfig.defconfig.

# USB stays available for flashing/charging, but there is no HID output in
# peripheral role. Uncomment to drop USB entirely and save a little power:
# CONFIG_ZMK_USB=n
